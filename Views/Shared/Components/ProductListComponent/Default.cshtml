@model List<Product>
<div class="container">
  <div class="row">
    @foreach (Product product in Model)
    {
      <div class="col-sm-@(12 / ViewBag.Columns)">
        <vc:product-component product=@product></vc:product-component>
      </div>
    }
  </div>
</div>